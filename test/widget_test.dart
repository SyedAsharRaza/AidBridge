/*
import 'package:aidbridge/app/app.dart';
import 'package:aidbridge/services/identity_store.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AidBridge app shell renders', (tester) async {
    final identityStore = IdentityStore();

    await tester.pumpWidget(
      AidBridgeApp(
        identityStore: identityStore,
      ),
    );

    expect(find.text('AidBridge setup'), findsOneWidget);
  });
}*/
